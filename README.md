# @tenjo/debug 🦕

A tiny JavaScript debugging utility for Deno.

It is based on [debug-js/debug](https://github.com/debug-js/debug), but modified
so that it only requires the permissions actually needed.

## Required Permissions

### Envrionment variables

- `DEBUG`
- `DEBUG_COLORS`
- `DEBUG_HIDE_DATE`
